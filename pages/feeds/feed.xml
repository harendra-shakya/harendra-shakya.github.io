---
layout: none
permalink: /feed.xml
---

<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ site.title }}</title>
    <link>{{ site.url }}</link>
    <description>{{ site.description | escape }}</description>
    <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
    <language>en-us</language>

    {% assign all_posts = site.posts | concat: site.bhajans | concat: site.poetry |
concat: site.insights | concat: site.sahitya | concat: site.avalokan | sort: 'date' | reverse %}

    {% for post in all_posts %}
      <item>
        <title>{{ post.title | escape }}</title>
        <link>{{ site.url }}{{ post.url }}</link>
        <guid>{{ site.url }}{{ post.url }}</guid>
        <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
        <description><![CDATA[
          {{ post.excerpt | strip_html | xml_escape }}
        ]]></description>
      </item>
    {% endfor %}
  </channel>
</rss>